{
  "name": "placementops-railway-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package.json to guide Railway to build the backend",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start"
  }
}
